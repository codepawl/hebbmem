# smolagents + hebbmem Memory

HuggingFace's lightweight agent framework with brain-like memory tools.

## Setup

1. Install deps: `pip install -r requirements.txt`
2. Run: `python agent.py`

Uses HF Inference API (free tier) by default.
